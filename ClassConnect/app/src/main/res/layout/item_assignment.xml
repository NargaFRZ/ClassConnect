<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:layout_marginBottom="8dp">

    <TextView
        android:id="@+id/txtAssignmentTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Assignment Title"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtAssignmentDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Assignment Description"
        android:textSize="14sp"
        android:textColor="#BBBBBB" />

    <TextView
        android:id="@+id/txtDaysUntilDue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Due in X days"
        android:textSize="14sp"
        android:textColor="#FF4081"
        android:visibility="visible" />

    <TextView
        android:id="@+id/txtSubmittedDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submitted on DATE"
        android:textSize="14sp"
        android:textColor="#FF4081"
        android:visibility="visible" />
</LinearLayout>
